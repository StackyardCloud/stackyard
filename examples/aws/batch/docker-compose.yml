services:
  stackyard:
    build:
      context: ../../../..
      dockerfile: Dockerfile
    container_name: stackyard
    ports:
      - "4566:4566"
    environment:
      - STACKYARD_LOG_LEVEL=debug

  batch:
    build:
      context: ../../../..
      dockerfile: examples/aws/batch/Dockerfile
    depends_on:
      - stackyard
    environment:
      - STACKYARD_ENDPOINT=http://stackyard:4566
      - STACKYARD_COMPUTE_ENV=batch-ce
      - STACKYARD_JOB_QUEUE=batch-queue
      - STACKYARD_SCHEDULING_POLICY=batch-policy
      - STACKYARD_JOB_DEFINITION=batch-jobdef

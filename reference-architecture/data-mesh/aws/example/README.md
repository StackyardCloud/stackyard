# Data Mesh Reference Architecture Example (AWS)

Canonical provider-scoped path for the AWS variant.

This example reuses the existing implementation at:

- `reference-architecture/data-mesh/example`

## Run

```bash
cd reference-architecture/data-mesh/aws/example
docker compose up --build
```

Open:

- Backend API: `http://localhost:8082`
